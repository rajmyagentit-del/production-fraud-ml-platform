services:
  - type: web
    name: production-fraud-ml-platform
    runtime: python
    plan: free

    buildCommand: pip install -e ".[api]"

    startCommand: >
      uvicorn fraud_ml.api:app
      --host 0.0.0.0
      --port $PORT

    healthCheckPath: /health
